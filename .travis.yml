language: cpp
compiler:
  - gcc
  - clang
os:
  - linux
  - osx
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libboost-dev
script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make all
  - test_all
  - cd ..
